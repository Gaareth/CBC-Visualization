# [[repos]]
# repo = "https://github.com/pre-commit/pre-commit-hooks"
# rev = "v6.0.0"
# hooks = [
#   { id = "check-yaml" },
#   { id = "end-of-file-fixer" },
# ]


[[repos]]
repo = "local"
hooks = [
    { id = "prettier & eslint", name = "prettier & eslint", entry = "pnpm lint:fix", language = "system", pass_filenames = false },
    # { id = "vitetest", name = "vitest", entry = "pnpm test:unit -- --run", language = "system", pass_filenames = false },
]
